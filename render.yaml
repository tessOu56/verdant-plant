services:
  - type: web
    name: verdant-platform
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: HOSTED
        value: "1"
      - key: NODE_VERSION
        value: "20"
